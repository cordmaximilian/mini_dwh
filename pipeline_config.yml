models:
  - name: wheat_weather
    active: true

sources:
  - name: commodities
    fetcher: sources.commodities.fetch
    schedule: "hourly"
    models:
      - wheat_weather
  - name: weather
    fetcher: sources.weather.fetch
    schedule: "hourly"
    models:
      - wheat_weather
